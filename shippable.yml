# Language setting
language: node_js

# Version number
node_js:
  - 0.12
    
build:
  ci:
    - if [[ $COMMIT_MESSAGE == *"full-test-suite"* ]]; then echo "works"; fi
    - if [[ $COMMIT_MESSAGE != *"full-test-suite"* ]]; then echo "no works"; fi

